export interface newJob {
  employerId: number;
  title: string;
  description: string;
  companyLocation: string;
  ctc: string;
  postedDate: string;   
  expiryDate: string;
  category: string;
  isActive: boolean;
}